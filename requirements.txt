pandas>=2.1
pyarrow>=15
numpy>=1.26
requests>=2.31
tqdm>=4.66
rapidfuzz>=3.6
sentence-transformers>=2.6
faiss-cpu>=1.7.4
matplotlib>=3.7          # fairness-utility frontier figure (12_...)
streamlit>=1.30          # fairness scorecard app (app.py)
# --- Experiment B (generative faithfulness) — needed once an LLM key is set ---
# anthropic>=0.40        # cited-answer generation + LLM-as-a-judge (Claude)
# ragas>=0.1             # RAGAS faithfulness scoring
